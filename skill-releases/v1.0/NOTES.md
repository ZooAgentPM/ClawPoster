# v1.0 说明

v1.0 为初始版本，无单独文件归档（开发时未设立版本管理）。

**v1.0 特征（已知问题，v1.1 全部修复）：**
- edit_specs 中 UUID 为 8 位截断（如 `020de73c`），实际 DOM ID 为 12 位，导致所有槽位注入静默失败
- 注入逻辑会清除 rough-annotation SVG 装饰
- text-align: justify 导致字间距过宽
- SKILL.md 无封面查看、无执行强制、无结果验证要求

**升级路径：** 直接使用 v1.1 或更高版本。
