---
name: visual-rag-design
description: 自动生成设计海报/图片。当用户提出设计需求（如「帮我做个促销海报」「做一张活动通知图」「生成一张小红书封面」）时使用此技能。通过 visual-rag MCP 服务直接生成 PNG 图片，图片实时返回对话，无需管理文件或服务。
---

# 虾稿设计

使用 `visual-rag` MCP 工具生成海报。首次使用先确保服务启动，然后选模板、查槽位、渲染。

渲染结果返回两张图：**render**（成品）+ **inspect**（各元素 UUID 标注）。看图有问题就直接带 adjustments 重渲，不要询问用户。

---

## 连接 MCP 服务

MCP 服务地址：`https://syncopated-retractively-anitra.ngrok-free.dev/mcp`

若工具不可用或域名失效，告知用户联系**小邹**获取新地址，不要自行猜测或重试。

---

## 选模板

共 95 套模板，三大分类：

| 分类 | 尺寸 |
|------|------|
| 小红书封面 | 1242×1656 |
| 手机海报 | 1242×2208 |
| 小红书配图 | 1242×1656 |

**场景速查**（直接用 ID 快速定位）：

| 场景 | 代表模板 ID |
|------|------------|
| 爆款/话题封面 | 708, 703, 699 |
| 干货攻略/清单 | 689, 183, 179 |
| 节日/促销 | 905, 337, 869 |
| 机构通知/招聘 | 677, 12, 278 |
| 情感/日签语录 | 672, 461, 871 |
| 对比/讨论 | 685, 896, 909 |

---

## 槽位填写规则

- `must_edit: true` 的槽位必须填写，其余保留原值
- `line_break: true` → 手动加 `\n` 换行，不能靠自动折行
- `list_line_count: N` → 必须填满 N 条，少了会露出裸底线
- 参考 `current`（原始示例）控制每行字数，不要大幅超出 `max_per_line`

---

## 布局微调（adjustments）

inspect 图左上角显示 UUID 前 8 位，用于定位元素。

支持 style 属性：`transform`（translateX/Y, scale）/ `display: none` / `opacity` / `fontSize` / `top` / `left`

---

## 品牌模板（t672 / t677）

- 文字品牌 → 填写 `logo_text` 槽位即可
- 图片 Logo → adjustments 隐藏内置图标，渲染后用 Pillow 叠加到品牌区域（坐标参考 inspect 图）

注意：每次重渲后必须重新叠加图片 Logo，不能复用上次的 final 图。

---

## 交付

渲染完成后必须做：
1. 把图片预览直接展示给用户（render 图）
2. 把下载地址告诉用户（响应里的「下载render」链接）

**下载地址必须使用响应中返回的链接，不得自行构造或猜测文件名。**

也可以直接把响应中的 base64 数据解码存为本地文件，不依赖下载链接：
```python
import base64
with open("poster.png", "wb") as f:
    f.write(base64.b64decode("<响应 data 字段>"))
```

---

## 原则

- **给成品不给过程**：自行比较调整方案，选最优后才展示给用户
- **内容优先**：内容多就换 heavy 密度模板，不要删减用户内容
